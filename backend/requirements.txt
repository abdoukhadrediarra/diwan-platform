Django>=5.1,<7
djangorestframework>=3.15
psycopg[binary]>=3.2
weasyprint>=62      # PDF des poèmes (HTML/CSS -> PDF, texte arabe correct)
python-docx>=1.1
whitenoise>=6.7         # serves the admin CSS/JS (fixes the unstyled admin)
gunicorn>=23.0          # the server used when the site is online
dj-database-url>=2.2    # reads DATABASE_URL, the address the hosts provide
django-cors-headers>=4.4 # lets the website call the API from its own address
flask>=3.0              # only for tools/poem_json_web
